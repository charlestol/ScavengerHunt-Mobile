import React, { Component } from 'react';  
import { View, Text } from 'react-native';

export default class ActiveEvents extends Component {  
  render() {
    return (
      <View>
        <Text>Active Events</Text>
      </View>
    );
  }
}